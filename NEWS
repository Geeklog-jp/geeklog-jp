NEWS

							Geeklog Japanese

このファイルはGeeklog日本語版のリリースに関する情報を簡潔に紹介するものです。


Geeklog 1.7.0-jp-1.2					2010年8月12日

  o Geeklog 1.7.0-jp-1.1からの変更

	* テーマWAIproCSS、ProfessionalCSSにおいて、fieldset legendに標準的な
	  スタイルを追加し、タイトルが2度読みになる箇所
	  (class="basic"が指定された箇所)のfieldset,legendのスタイルをすべて
	  非表示に設定しました。

	* テーマProfessionalCSS、WAIproCSS で、OpenID用ログインフォームが表示
	  されないのを修正しました。
	  users/loginform.thtmlへ{openid_login}を追加しました。

	* テーマProfessionalCSS で、スタイルを変更しました。
	  common.cssのinput,select,textareaで日本語のフォントスタイルを追加しました。
	  コメントの英語化をコミットする際に変更しなくても良いfont-familyを変更して
	  いました。

	* テーマProfessionalCSS で、スタイルを変更しました。
	  1. legendが非表示になる初期設定を廃止しました。
	  2. IE専用のワード途中で折り返しさせるスタイル th,td{word-break: break-all;}
	     を削除して、折り返しなしのデフォルトに戻しました。

	* すべてのテーマのテンプレートファイルsubmit/submitloginrequired.thtml
	  は不要になったので削除しました。

	* テーマWAIproCSSのcss/compatible.css で、
	  リンクの背景画像に指定しているexternalとfeedアイコンが
	  アクティブ・ホバー・フォーカス時に消えてしまうので、修正しました。

	* テーマProfessionalCSS WAIproCSSのcss/block.cssの
	  右ブロックのヘルプアイコンのセレクタを修正しました。

	* r2672の修正を確認し、テーマWAIproCSSのcss/compatible.cssの
	  背景画像をgifに変更し、アイコンがきちんと表示されるよう
	  余白の修正をしました。

	* テーマProfessionalCSS WAIproCSS のcss/compatible.css で、画像ファ
	  イルのリンクエラーを修正しました。

	* テーマProfessionalCSS/css/default.cssの、
	  url(../javavscript/iepngfix.htc)をコメントアウトしていましたが、
	  これを削除しました。

	* テーマProfessionalCSS mobile WAIproCSSにimages/update.pngを追加
	  しました。

	* テーマWAIproCSSでカスタムメニュー有効時にグローバルナビの文字色が
	  基本色になってしまうのを修正しました。

	* 日本語ドキュメントの表現長期保存クッキー名をパーマネントクッキー名に
	  変更しました。

	* ソース内のウムラウト表記で文字化けしていたものを修正しました。

	* 日本語ドキュメントの表現を調整しました。

	* プラグイン管理画面の説明文とアップデートを要求する文言を修正しました。

	* ドキュメント変更履歴のタイトルおよびリンクテキストを修正しました。

	* 静的ページのコンフィギュレーションにおける、管理者ページのリストソート順
	  sort_list_byの翻訳を修正しました。

	* 'Permalink'を'永久リンク'、'パーマネントリンク'と翻訳していたのを、
	  'パーマリンク'に統一しました。

	* Googleスタイルの検索結果表示でon日時と表示されていたのを日時だけに
	  するよう、言語ファイルを修正しました。

	* 日本語言語ファイルの、プラグイン管理画面で「更新されています」を、
	  英語版にあわせて「アップデート!」に変更しました。

	* README.jpに、ファイルREADME.jpの情報を追加しました。

	* インストール時、移行時のインポート中のメッセージを変更するため、
	  インストーラの言語ファイルを修正しました。

	* NEWSからREADME.jpを分離しました。

	* 日本語ドキュメントのテーマ作成（theme.html）の1.7.0の翻訳を追加し、
	  書式を一部揃えました。


  プラグイン

	* アンケートの日本語言語ファイルで、新着情報ブロックの表現を調整しました。

	* DbmanプラグインでTIMESTAMPフィールをクォートして保存していなかった問
	  題(Issue #158)に対処しました。

	* Themeditプラグインで一部のHTMLタグがXHTMLに対応していなかったバグ
	  (Issue #153)を修正しました。

	* 掲示板プラグインで、有効期間内であるにもかかわらずセキュリティトークン
	  が失効するバグ（の一部）に対応しました。

	* 掲示板プラグイン(forum)の言語ファイルで、Submitを、保存と翻訳されていたのを
	  保存する、に変更しました。他、タブメニュー部で折り返されやすいので言語を
	  短く調整しました。

	* tkgmapsを0.9.4から2.0.4にアップしました。


  o Google CodeのIssueの情報

	Geeklog 1.7.0-jp-1.2で修正されたIssueは以下から参照できます。

   http://code.google.com/p/geeklog-jp/issues/list?can=1&q=label%3A1.7.0-jp-1.2
